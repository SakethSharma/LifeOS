import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sakethsharma.lifeos',
  appName: 'LifeOS',
  webDir: 'dist/demo/browser'
};

export default config;
